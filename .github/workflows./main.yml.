name: CI_Pipeline
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build Backend Docker
        run: docker build ./backend -t backend-check

      - name: Build Frontend Docker
        run: docker build ./frontend -t frontend-check